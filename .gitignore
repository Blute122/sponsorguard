__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# Real emails used for evaluation. They contain PII and must never be committed.
# See eval/README.md for the redaction rule.
eval/corpus/private/

# Collection tool output: raw personal mail awaiting manual triage, plus the
# dedupe store. Never commit either. See eval/tools/README.md.
eval/corpus/sponsorship/review/
eval/tools/.seen_ids

# Local credential/config files (GMAIL_APP_PASSWORD etc). Never commit.
.env
*.env

# Threat-intel feed snapshots: cache-only, never committed (large, licence-bound).
.threatintel_cache/
# Optional Google Safe Browsing key (SPONSORGUARD_GSB_API_KEY) lives in env/.env.
